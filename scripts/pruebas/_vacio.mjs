/**
 * Módulo vacío a propósito.
 *
 * Sustituye a `server-only` cuando las pruebas importan código de servidor.
 * Ese paquete no tiene comportamiento: es una señal para el compilador de
 * Next. Ver `_resolver.mjs`.
 */
export {};
